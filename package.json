{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "rm -rf packages/*/lib",
    "watch": "tsc --build --watch",
    "build": "tsc --build"
  },
  "devDependencies": {
    "typescript": "^3.2.2"
  }
}
